import React from 'react';
import AbstractMessage from '../AbstractMessage';

const Question = ({ message }) => <AbstractMessage
    message={message}
    className='Question'
/>

export default Question;