import React from 'react';
import AbstractMessage from '../AbstractMessage';

const Response = ({ message }) => <AbstractMessage
    message={message}
    className='Response'
/>

export default Response;